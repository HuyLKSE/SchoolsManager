<!DOCTYPE html>
<html lang="vi">

<head>
    <%- include('./head', { title: typeof title !=='undefined' ? title : 'QLHS' }) %>
</head>

<body
    class="bg-gradient-to-br from-blue-50 via-white to-blue-50 font-sans antialiased text-gray-900 selection:bg-blue-500 selection:text-white overflow-x-hidden">
    <div class="flex min-h-screen">
        <!-- Sidebar -->
        <%- include('./sidebar', { user: typeof user !=='undefined' ? user : null, currentPath: typeof currentPath
            !=='undefined' ? currentPath : '' }) %>

            <!-- Main Content Wrapper -->
            <div class="flex-1 flex flex-col min-w-0 lg:pl-72 transition-all duration-300">
                <!-- Navbar -->
                <%- include('./navbar', { user: typeof user !=='undefined' ? user : null, title: typeof title
                    !=='undefined' ? title : 'Dashboard' , currentPath: typeof currentPath !=='undefined' ? currentPath
                    : '' }) %>

                    <!-- Main Content -->
                    <main class="flex-1 p-4 sm:p-6 lg:p-8">